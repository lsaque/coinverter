import styled from 'styled-components';

export const Container = styled.div`
  height: 30vh;

  background:
    linear-gradient(
      205deg,rgba(14, 8, 35, 0.4) 0%,
      rgba(60, 31, 139, 0.4) 100%
    );

  box-shadow: rgb(255 255 255 / 20%) 0px 0.5px 0px 0px inset;
`;
